# Core ML / DL
# CPU-only torch index — avoids pulling the large CUDA build (~3.5GB) from PyPI.
# Works for both local dev and Docker builds.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.36.0
safetensors>=0.4.0

# Image processing
Pillow>=10.0.0
pypdfium2>=4.0.0
opencv-python-headless>=4.8.0

# API layer
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
websockets>=12.0.0

# Data + DB
numpy>=1.24.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0
minio>=7.2.0

# Background queue
redis>=5.0.0
rq>=1.16.0
